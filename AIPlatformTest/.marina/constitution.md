# Constitution

Define the non-negotiable principles that every Marina agent must follow.
These principles are injected into every agent prompt as spec context.

## How to use

Replace the examples below with your project's real constraints.
Each principle should be a clear, enforceable rule.

## Principles

- [ ] Example: All API endpoints must require authentication.
- [ ] Example: Database migrations must be backwards-compatible.
- [ ] Example: No secrets in source code — use environment variables.
